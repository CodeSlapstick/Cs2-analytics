/**
 * ต้อง import ไฟล์นี้เป็น "บรรทัดแรกสุด" ของเทสต์ที่แตะฐานข้อมูล
 *
 * เหตุผล: ESM ประมวลผล import ทั้งหมดก่อนโค้ดบรรทัดแรกของไฟล์เสมอ ถ้าเขียน
 * `process.env.PGLITE_DIR = 'memory://'` ไว้บนสุดของไฟล์เทสต์ตรง ๆ มันจะทำงาน
 * *หลัง* config.js อ่าน env ไปแล้ว ผลคือเทสต์ไปเขียนทับฐานข้อมูล dev จริง ๆ
 * (เจอมาแล้วตอนเขียนเทสต์ชุดนี้ — เทสต์เห็นแมตช์ 13 นัดจากที่ seed ไว้)
 */
process.env.PGLITE_DIR = 'memory://';
process.env.DATABASE_URL = '';
process.env.NODE_ENV = 'test';

# Parser service — ใช้ Python 3.11 ขึ้นไป
# ติดตั้ง:  pip install -r parser/requirements.txt
#
# awpy 2.x ใช้ demoparser2 (เขียนด้วย Rust) อยู่ข้างใน จึงมี wheel สำเร็จรูปให้
# บน Windows ไม่ต้องลง Visual Studio Build Tools เอง
awpy>=2.0.2
polars>=1.0

# map_zones.py — แบ่งโซนด้วย unsupervised learning (KMeans + DBSCAN)
scikit-learn>=1.5
numpy>=1.26

# check_radar_calibration.py — วาดจุดคิลทับภาพเรดาร์เพื่อตรวจค่าปรับเทียบ
pillow>=10.0

# -*- coding: utf-8 -*-
# เครื่องมือตอนพัฒนา — ไม่ต้องมีตอนรันระบบจริง
#   pip install -r requirements-dev.txt     (ไฟล์นี้ดึง requirements.txt มาด้วย)
-r requirements.txt

pytest>=8
pytest-asyncio>=0.23
ruff>=0.5
vpk>=1.4          # research/import_overviews.py อ่านค่าปรับเทียบเรดาร์จากไฟล์เกม CS2
